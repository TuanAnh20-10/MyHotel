<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" /> <!-- Màu nền -->
    <stroke
        android:width="2dp"
        android:color="#8261f0" /> <!-- Viền -->
    <corners android:radius="10dp" /> <!-- Góc bo tròn -->
</shape>
